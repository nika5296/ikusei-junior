---
description: コード修正後は必ず実行確認まで行う
globs: "**/*"
---

# 修正の完了条件

- コードを直したら **説明だけで終わらせない**。可能な範囲で **実行まで** 行う。
- **parent-portal** を触った場合は少なくとも `parent-portal` で `npm run build` を実行し、エラーがないことを確認する。
- 本番反映が意図に含まれる場合は、プロジェクトの手順に従い **Vercel デプロイ等** も実行する（認証・環境が必要ならその結果を報告する）。
